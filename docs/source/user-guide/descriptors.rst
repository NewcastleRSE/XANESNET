Descriptors
===========

Overview
--------

Descriptors featurise structures into fixed-size vectors for descriptor-based
datasets. Descriptors are configured under ``dataset.descriptors``  in the YAML configuration file; 
each block sets a descriptor type and its specific parameters.

Available descriptor types
--------------------------